export enum PaymentStatus {
  Pending = "pending",
  Confirmed = "confirmed",
  Reject = "rejected",
}
export enum ReservationStatus {
  Pending = "pending",
  Confirmed = "confirmed",
  Reject = "rejected",
}
